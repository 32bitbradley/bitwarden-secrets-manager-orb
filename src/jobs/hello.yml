description: >
  Sample description
# What will this job do?

executor: default

parameters:
  secret_id:
    type: string
    description: "The secret ID to retrieve"

steps:
  - retrieve:
      secret_id: << parameters.secret_id >>
