description: >
  Read secrets [revipusly exportted to the secrets file and export as environment variables to $BASH_ENV

parameters:
  secrets_dir:
    type: string
    description: "The directory to store retrived secrets"
    default: ".secrets"
  secrets_file:
    type: string
    description: "The file within the secrest director to storer secrets in"
    default: "secrets.env"

steps:
  - run:
      name: Export secrets to BASH_ENV
      command: cat ./<< parameters.secrets_diry >>/<< parameters.secrets_file >> >> $BASH_ENV
