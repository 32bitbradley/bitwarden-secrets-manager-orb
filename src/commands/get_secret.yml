description: >
  gets a specific secret  bitwarden secrets manager

parameters:
  secret_id:
    type: string
    description: "The secret ID to get"
steps:
  - run:
      environment:
        PARAM_SECRET_ID: <<parameters.secret_id>>
      name: Displaying secret ID
      command: <<include(scripts/get_secret.sh)>>
