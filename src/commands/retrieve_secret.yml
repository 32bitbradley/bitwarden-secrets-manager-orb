description: >
  Retrieves a specific secret  bitwarden secrets manager

parameters:
  secret_id:
    type: string
    description: "The secret ID to retrieve"
steps:
  - run:
      environment:
        PARAM_SECRET_ID: <<parameters.secret_id>>
      name: Displaying secret ID
      command: <<include(scripts/retrieve_secret.sh)>>
